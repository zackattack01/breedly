Breedly.Collections.FeedTopics = Backbone.Collection.extend({
  // url: 'api/feeds/' + this.feed.get('id') + 'feed_topics',
  
  initialize: function(options) {
    // this.feed = options.feed;
  }
});