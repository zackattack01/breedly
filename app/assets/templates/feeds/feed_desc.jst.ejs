<div class="desc-layer-bg"></div>
<div class="teaser-content">
  <h2 class="feed-title"><%= feed.get('title') %></h2>
  <h3 style="color:#A11111"><%= feed.get('description') %></h3>
    <% var topics = feed.topics(); %>
    <h4>Topics</h4>
    <% if (topics.length > 0) { %>
      <ul style="list-style-type:disc">
        <% topics.each(function(topic) { %>
          <li class="topic-list-item"><%= topic.get('title') %></li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>There are currently no topics associated with this feed.</p>
    <% }; %>
</div>