<ul class="entries-list nav">
  <% entries && entries.forEach(function(entry, idx){ %>
    <li class="<%= idx === activeIdx ? "active-entry" : "" %>">
      <a href="#" data-idx="<%= idx %>" class="entry-list-item">
      <%= entry.title %></a>
    </li>
  <% }); %>
</ul>